{
    "name": "node-hello-world",
    "version": "1.0.0",
    "description": "Hello World",
    "scripts": {
        "dev": "parcel serve src/index.html --open",
        "build:do": "parcel build src/index.html -d build --public-url /",
        "deploy": "git subtree push --prefix build origin gh-pages"
    }
}